{
  "name": "nestjs-dummy-module",
  "author": "Eric Lara <ericzon@gmail.com>",
  "version": "0.1.3",
  "description": "Example of dummy module working with NestJs",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build",
    "prepublishOnly": "npm run clean && npm run build",
    "clean": "rm -rf dist",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["nestjs", "module"],
  "license": "MIT",
  "dependencies": {
      "@nestjs/common": "^3.0.0",
      "@nestjs/core": "^3.0.1",
      "@nestjs/microservices": "^3.0.1",
      "@nestjs/websockets": "^3.0.0",
      "rxjs": "^5.4.3",
      "reflect-metadata": "^0.1.10"
  }
}
